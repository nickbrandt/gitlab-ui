import { shallowMount } from '@vue/test-utils';
import Gl{{pascalCase name}} from '{{pathToRootDir}}/src/components{{componentDir}}/{{name}}.vue';

describe('Gl{{pascalCase name}}', () => {
  it('renders main components', () => {
    const wrapper = shallowMount(Gl{{pascalCase name}});
    expect(wrapper).toBeInstanceOf(Object);
  });
});
